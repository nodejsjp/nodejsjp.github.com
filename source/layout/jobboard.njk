{% extends layoutDir + '/layout.njk' %}

{% block main %}
<h1>ジョブボード</h1>

{% for job in file.files %}
<hr />
{% if job.fm.class == 'platinum' %}
  <h1><a href="{{ basepath(file) }}/{{ job.relative }}">{{ job.fm.name }}</a></h1>
{% elif file.fm.class == 'gold' %}
  <h2><a href="{{ basepath(file) }}/{{ job.relative }}">{{ job.fm.name }}</a></h2>
{% else %}
  <h3><a href="{{ basepath(file) }}/{{ job.relative }}">{{ job.fm.name }}</a></h3>
{% endif %}
{% if job.fm.image %}
  <figure>
    <a href="{{ basepath(file) }}/{{ job.relative }}">
      <img src="{{ basepath(file) }}/{{ job.fm.image }}" class="job-logo">
    </a>
  </figure>
{% endif %}
<p>
{% if job.fm.role %}
  募集職種:
  {% for role in job.fm.role %}
    {% if not loop.first %}/{% endif %}
    {{ role }}
  {% endfor %}
  </p>
{% endif %}
{% endfor %}

{% endblock %}
